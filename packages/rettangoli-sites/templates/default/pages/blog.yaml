---
template: base
title: Blog
---
- h1: Blog

- div.posts:
    - $for post in collections.blog:
        - article:
            - 'a href="${post.url}"':
                - h2: ${post.data.title}
            - p.meta: ${post.data.date}
