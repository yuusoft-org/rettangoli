import { generateCSS } from '../common.js'

const styles = {
    "cur": {
        "p": "pointer",
        "m": "move",
        "grab": "grab",
        "grabbing": "grabbing",
    },
};

export default generateCSS(styles);