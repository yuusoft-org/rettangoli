# VT Benchmark Fixture (Basic)

Minimal fixture used by `scripts/benchmark-capture.js`.

- Config: `rettangoli.config.yaml`
- Specs: `vt/specs/components/*.yaml`

Run from `packages/rettangoli-vt`:

```bash
bun run bench:capture
```

Custom runs:

```bash
bun run bench:capture --runs 10
```
