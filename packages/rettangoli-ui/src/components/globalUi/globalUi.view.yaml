elementName: rtgl-global-ui

viewDataSchema:
  type: object
  properties:
    isOpen:
      type: boolean
    config:
      type: object
      properties:
        status:
          type: string
        title:
          type: string
        message:
          type: string
        confirmText:
          type: string
        cancelText:
          type: string
        mode:
          type: string

refs:
  dialog:
    eventListeners:
      close:
        handler: handleDialogClose
  confirm-button:
    eventListeners:
      click:
        handler: handleConfirm
  cancel-button:
    eventListeners:
      click:
        handler: handleCancel

template:
  - rtgl-dialog#dialog ?open=${isOpen} s=sm:
      - rtgl-view slot=content g=lg p=lg:
        - rtgl-view d=h g=md:
          - rtgl-view d=h ah=c av=c g=md:
            - rtgl-svg svg=info wh=22:
          - rtgl-view g=md:
            - rtgl-view h=24 av=c:
              - rtgl-text fs=lg fw=600: ${config.title}
            - rtgl-text: ${config.message}
        - rtgl-view d=h jc=fe g=md mt=lg w=f ah=e:
          - $if config.mode == 'confirm':
            - rtgl-button#cancel-button v=se: ${config.cancelText}
          - rtgl-button#confirm-button v=pr: ${config.confirmText}