refs:
  input:
    eventListeners:
      value-input:
        handler: handleValueInput
      value-change:
        handler: handleValueChange
  slider:
    eventListeners:
      value-change:
        handler: handleValueChange
      value-input:
        handler: handleValueInput
template:
  - rtgl-view d=h av=c g=md w=${w}:
      - rtgl-slider#slider aria-label=${ariaLabel} aria-description=${ariaDescription} aria-invalid=${ariaInvalid} key=${key} w=f type=range min=${min} max=${max} step=${step} value=${value} ?disabled=${disabled}: null
      - rtgl-view w=84:
          - rtgl-input-number#input aria-label=${ariaLabel} aria-description=${ariaDescription} aria-required=${ariaRequired} aria-invalid=${ariaInvalid} key=${key} w=f min=${min} max=${max} step=${step} value=${inputValue} ?disabled=${disabled}: null
