
 # Quickstart

run with npm

```bash
npm i -g rtgl
rtgl fe build
npx serve static
```

run with bun

```bash
bun i -g rtgl
rtgl fe build
bunx serve static
```

run from local

```bash
bun run ../../packages/rettangoli-cli/cli.js fe build
bunx serve static
```
