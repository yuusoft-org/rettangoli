# TODO a way to configure dev and prod
# all rtgl fe configuration
fe:
   dirs: ['fe/components', 'fe/pages'] # directories that contain components
   setup: 'fe/setup.js' # path to setup.js file
   outfile: 'dist/main.js' # output this is separate from rettangoli file
  #  watchPort: 3001

# all sites configuration
# sites:
#   path: 'sites'
#   templateFolder: '..'

# all visual testing configuration
# vt:
#    path: 'vt
